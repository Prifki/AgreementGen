## An agreement and a receipt generator for applicants of ITMO University

Realised on NodeJs, Bootstrap and SQLite

### Dependencies:
* `$npm install sqlite3`
* `$npm install --save jsonfile`

### To Run:
* `$node index.js`